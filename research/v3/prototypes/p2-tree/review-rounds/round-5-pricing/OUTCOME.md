# phase2-pair-024 — verified decode and outcome (the five priced trades)

Analyst verification: **PASS** — all 5 items confirmed three ways; 15 raw → 5 resolved
(keystroke supersessions only); parity matches ROUND.md; all sides pinned `734c3f5`. Two
schema flags recorded: v3.0 carries no `veto` field (guards read `confound`, all false);
item 5's preference is `prefilled`-sourced.

| item | sides | grades | pref | note (verbatim, abridged) |
|---|---|---|---|---|
| 1 accent lever | `#d25068` vs `#ee5567` | weak / weak | none | "both are missing the dark green, the grey… Mothy for the foreground which i do not see… background that is not the background" |
| 2 fg lever (near case) | `#070506` vs `#050304` | **strong / strong** | none | — |
| 3 coverage (chroma trade) | `#c18d20` vs `#362545` | strong / strong | none | — |
| 4 coverage (family/lightness) | `#fea851` vs `#242e09` | unacceptable both | none | "unreadable foreground on background" |
| 5 low-contrast text | `#f8dab8` vs `#16151a` | unacceptable / acceptable | **#16151a** | "should be a gradient; side B: foreground is unreadable" |

Firing rows (mechanically MECE): I1-N · I2-N · I3-N · I4-N (with the analyst's flag that its
declared rationale is not what the note says — the failure is on both sides and orthogonal
to the priced trade) · I5-V · J-0b (allocation integration **undecided**: 0/2 allocated
preferred, both no-preference — no integration, no redirection).

## What this settles (rulings in DECISIONS.md D18)

1. **The dither-stability levers are FREE.** Within-family cluster-member substitution drew
   no preference at bar-scale distances on both roles (items 1–2, item 2 at both-STRONG).
   Worker J's measured stability variants (chroma-extremal accent members: dither churn
   collapse; contrast-extremal fg) are now adoptable per the pre-declared I1-N/I2-N actions.
2. **Coverage allocation stays uninstalled** (J-0b): the reviewer expressed no preference on
   either adverse case; D17's analyst flags stand (item 3's allocated colour moved the
   accent INTO the ground family — the disfavoured direction — without complaint; item 4's
   failure is fg readability, not the trade).
3. **Item 5:** identity does not outrank legibility at |APCA| 2.7 (I5-V: measure-then-
   calibrate; nothing ships). Plus the campaign's first direct missed-gradient reviewer
   datum ("should be a gradient") — routed to the gradient work now that the gate is open.
4. **Item 1's note is a coverage/identity complaint on a stability item** (missing dark
   green and grey; a foreground colour "I do not see in that artwork") — filed as D17
   evidence for the allocator's eventual figure-vs-ground redesign, and as another fg
   colour-recall datum.
