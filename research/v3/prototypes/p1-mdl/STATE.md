# P1 — where we're at (updated 2026-08-11: KILLED per pre-registered condition)

**FINAL: the v1 keep-or-kill round (phase2-pair-019) fired the kill condition** — the dominant
verdict class demands role-assignment pricing this mechanism cannot express (swap-indifference
is structural). Split finding: λ-repaired arm a earned P1's first two STRONG grades and 7/7
preference; the chromatic residual stayed zero-strong with its target complaint class grown. The
v0 inks falsifier did NOT repeat (zero readability complaints in v1). Reviewer authored an
endorsed-sample on item 5 — preserved as ground truth. Sections below are the pre-round state;
§5's graft list is the living value. Full verdict record: DESIGN.md §V1; ANALYSIS.md in
review-rounds/v1-keep-or-kill/.

# (pre-pause state, 2026-08-05)

## 1. Mechanism

One scalar description-length objective over the WHOLE contract configuration (four roles,
gradient boolean, stops, collapse flags), minimised jointly — no stages, no candidate lists, no
per-role filters; the contract is the feasible set, never a term. Two priors under internal
experiment: **p1a** (nats — mixture code, uniform-gamut residual, extent-based field/ink) and
**p1ap** (bits — E = L(pixels|P) + λ·L(P), the contract's own serialization as the prior).

## 2. Judge-validated strengths (round provenance)

Thin, honestly: round `phase2-pair-010` (8 items, both priors, v0) produced ZERO strong grades.
What the judge did validate: the one isolated-ramp pairing (item 5) was won by the A′ ramp and
drew no gradient complaint; all three emitted ramps were 2-stop — the shape every cross-arm
round rewarded — and measured on-artwork clean (excursion probe, max sample 83% of its bar); no
epsilon-margin sitting (min role-pair distance ≈ 2× bar; the failure that killed another arm's
round is structurally absent here — collapse eats would-be epsilon pairs). Everything else below
is instrument-validated, not judge-validated.

## 3. Measured weaknesses, located causes (v0 falsified; v1 repairs measured so far)

- **v0 FALSIFIED (filed, stands):** ink-recovery bet fired ("accent and foreground are
  impossible to distinguish", our min-|APCA| 2.55); coverage-vs-identity — `genericBits` was
  78–99.9% of A′'s energy and its best grades went to its LEAST-covering palettes (corroborating
  M1's legacy-corpus finding: the residual paid for coverage, the reviewer pays for identity);
  arm A fully degenerate at λ=1 (two-colour palettes 20/20).
- **v1 repairs, measurably landed (unjudged):** chromatic residual (A′ 0.2.0) — naming decided
  by chromatic isolation, pixel counts deleted; reproduces the round's exact complained pattern
  in test (white/yellow/red/black artwork → 4-role argmin, yellow background); verified
  bit-exact by independent re-derivation (V5), zero new constants. λ_A = 0.1 [MEASURED, 72-cell
  probe]: λ-priced degeneracy confirmed dominant and repriced; early re-emission shows mixed
  structure (collapsed/two-flat/first ramps) vs v0's 20/20 collapse.
- **Unrepaired, known, out of granted scope:** A′ rendition instability (4/8 pairs moved, worst
  36× bar; p1a was 8/8 stable); arm A's data-term generosity (2/6 covers collapse at every
  probed λ even at 4× budget); the indistinguishable-inks axis (contract floors are the
  reviewer's; deliberately untouched — if v1 dies there again, that is kill data).

## 4. Open questions and cost

**Keep-or-kill round: PENDING** — v1 re-emission running (detached; ~2.5 h), then robustness
screen (--limit 8/arm), then the ONE granted 8-item pairwise round (same covers as v0). Kill
condition pre-registered: needing anything beyond the recorded repair kills. Open behind it:
stop-buying propensity under fuller search (a −212-bit reachable stop existed; 32-bit stop cost
is negligible at λ=1 — banding risk rule in place); v1 robustness unmeasured. Cost so far: days
(measure 1d, energies ~2d, falsifier+attribution ~1d, v1 repairs ~1d). If kept: certified
branch-and-bound search is the priced next step (~4–5 days, arm-a §6).

## 5. Graftable components (usable by other arms / the bake-off regardless of verdict)

- **Score-any-palette energy** — both priors evaluate arbitrary contract-legal palettes with
  per-term breakdowns; ran the 554-entry legacy falsifier; a candidate currency for P4's
  selector; could score other arms' outputs today.
- **Measurement layer** — exact triple table + integer moments + dyadic extent ladder + smoothed
  mass + closed-form geometries; verified bit-exact against naive re-derivation; ~0.8 s/image.
- **Excursion probe** — measures whole-ramp on-artwork-ness against the contract's own
  interpolator; the campaign's excursion instrument is otherwise unbuilt.
- **Round-staging kit hardening** — id-surface check with negative test (caught two leak classes
  beyond the incident that prompted it); KEY continuity pattern across restage/retirement.
- **L(P) serialization coster; margin-distribution probe; λ/budget disentangle probe design.**

## 6. Honest promise assessment

The currency as proposed mispriced what the reviewer grades, and that is proven twice over
(legacy corpus, then live round) — the strongest negative any arm has filed about itself. The
one granted repair is exactly at the located cause, reproduces the reviewer's demanded pattern
in fixture, and cost zero new constants; whether it prices REAL covers the way the reviewer
judges is precisely what the pending round decides, and nothing in hand licenses a prediction.
Independently of that verdict: p1a's rendition stability (8/8) is the best robustness number
this arm has seen on any prototype's output, the mechanism's structural properties (no candidacy
walls, collapse-eats-epsilon, certified-gap honesty, score-anything energy) held up under three
independent verification passes, and the graft list above is real value even on a kill. Promise,
plainly: as a WHOLE mechanism, unproven until the round returns; as a source of parts and of
falsification discipline, already earning.
