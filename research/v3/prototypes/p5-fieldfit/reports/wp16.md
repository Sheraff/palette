# W-P16 — v0.8.2: foreground class ordering (decision 18, scale mixing)

## 1. Anchors

**(a) `2376a6b67d` — fg `#000000` / accent `#f81107`**, round 3's verbatim ask. fg shortlist all class A;
the red (field mass 11 694; mortality 0.671 / adjacency 0.159 ⇒ ground) leads the accent shortlist at 8.2×
the black's 1 424. Coverage 3, `coverageDecided` false.

**(b) `908479200b` — NOT byte-identical. STOPPED for a ruling.** fg `#fed078` restored exactly (class A,
2 432; both regions class B, mortality 0.281 / 0.612). Accent `#412824`→`#231f20`: the accent ranks the
**full union by mass** — 24 591 vs 224 — which the ruling deliberately left alone, so the class ordering
cannot reach it. Owed: extend the class term to the accent (restores v0.7.1 here), or the union stands.

**(c) `28279e9184` — fg `#ffffff` NOT held (`#fafafa`), and not via the class term.** 18(a)'s dedupe
superseded the `#ffffff` cluster (175) when the white component (11 795) was admitted, so v0.7.1's fg is
not in the pool at all. Admissible: `#eaeaea` (A, 43) and `#fafafa` (B); **coverage** takes the component,
4 vs 2. The one cover where `classOverriddenByCoverage` fires.

**(d) `a8942d6547`, `eaed77a9cb` byte-identical.**

## 2. Deltas vs v0.8.1 — 5/27, every one a foreground

| cover | change |
|---|---|
| `2376a6b67d` | fg/ac swap → `#000000`/`#f81107` |
| `908479200b` | fg `#231f20`→`#fed078`, ac `#fd9f55`→`#231f20` |
| `fc8d58e0af` | fg/ac swap → `#201c13`/`#9ba193` |
| `c5ebb948ee7f1b` | fg/ac swap → `#ffffff`/`#d44509` |
| `91a16672c4` | fg `#d09d7e`→`#d8a685` (component → ink; coverage decides) |

## 3. Deltas vs v0.7.1 — 7/27, six accent-only

| cover | change | trace |
|---|---|---|
| `2376a6b67d` | ac `#000300`→`#f81107` | the ask |
| `908479200b` | ac `#412824`→`#231f20` | union by mass, §1(b) |
| `fc8d58e0af` | ac `#e0d0db`→`#9ba193` | component 5 728 vs 563 |
| `16a8247378` | ac `#263143`→`#736e6a` | component 5 233 vs 144 |
| `c5ebb948ee7f1b` | ac `#000000`→`#d44509` | component 6 982 vs 476 |
| `91a16672c4` | fg `#c39170`→`#d8a685`, ac `#3b4c56`→`#2c3a45` | coverage 2→3; component 44 510 |
| `28279e9184` | fg `#ffffff`→`#fafafa` | dedupe, §1(c) |

Every fg delta vs v0.7.1 is `91a16672c4` (coverage) and `28279e9184` (dedupe); no foreground moves by the
class term itself. The other 20 covers run the global fit and have no component.

## 4. Placement, measured not argued

Class sits at the head of the per-role tie-breaks, **below** coverage (decision 18's lexicography: the
ruling redefines the foreground ordering, coverage outranks the orderings). Alternative measured by
re-running the 27 with class above coverage: **one** cover changes — `28279e9184` fg `#fafafa`→`#eaeaea`,
which wp15 called worse — and 26 are identical. `classOverriddenByCoverage` is published per cover.

## 5. Coverage

0×1, 1×11, 2×5, 3×7, 4×3 — identical to v0.8.1. `coverageDecided` **2/27** (was 0/27): `28279e9184`,
`91a16672c4`.

## 6. Verification

**96/96 tests** (+5: class ordering ×3, the class-B-only foreground, anchor (c); anchors re-based with
traces, randomized enumeration fixtures now carry both classes). `tsc --strict` clean but the two
pre-existing contract errors. **Scorecard 0 violations, 3 sets, 27 covers.** `P5_COMPONENT_ROLES` deleted.
Robustness not run, as instructed.
