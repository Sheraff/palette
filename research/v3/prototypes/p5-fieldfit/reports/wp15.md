# W-P15 — v0.8.1: pool re-union (decision 18, ruling (a))

## 1. Anchor (a) — item 3: the red claims a role. It claims the **foreground**, not the accent

`2376a6b67d`: `#fad107`/`#f9fbf8`/**`#f81107`**/**`#000000`** (was `…/#000000/#000300`). Reviewer's four
colours, twin gone, coverage 2→3. Trace: one unslotted component (depth 1, support 0.138, centre
`#fc0402`), published `#f81107` by mass-snap **over its own support**, field mass **11 694**, feasible
vs published colours, `min|raw|` 43.7; supersedes its family's rejected-mass crumbs `#f20000`@47.5,
`#fe0000`@34.3 — v0.8.0's unpublishable entries. Both `fg=red/ac=black` and `fg=black/ac=red` are
feasible at coverage 3; the **fg-mass tie-break** decides (11 694 vs 1 424). `coverageDecided: false`.

**Finding: field mass and rejected mass are not one scale** — regions 5 000–84 000, inks 10²–10³, so a
component wins every mass tie-break and decision 13's "the artwork's own ink" becomes "the largest
unslotted region". Measured: `P5_COMPONENT_ROLES=accent` (knob; ships `both`).

## 2. Deltas — 7/27, all component covers, **one silent STRONG moves**

| cover | shipped (`both`) | `accent` column |
|---|---|---|
| `2376a6b67d` | fg→`#f81107`, ac→`#000000` | `…/#000000/#f81107` = reviewer's ask |
| **`908479200b`** STRONG | fg `#fed078`→`#231f20`, ac `#412824`→`#fd9f55` | fg held; ac→`#231f20` |
| `91a16672c4` | fg→`#d09d7e`, ac→`#2c3a45` | fg held |
| `fc8d58e0af` | fg→`#9ba193`, ac→`#201c13` | roles swapped |
| `c5ebb948ee7f1b` | fg→`#d44509`, ac→`#ffffff` | roles swapped |
| `16a8247378` | ac `#263143`→`#736e6a` (5 233 vs 144) | same |
| `28279e9184` | fg `#ffffff`→`#fafafa` | `#eaeaea` — worse |

One mechanism throughout. `a8942d6547`, `eaed77a9cb` byte-identical; the other 20 covers run the global
fit, have no component, cannot move.

## 3. Anchor (b) — item 6: no improvement, none available

`9646be9b20` unchanged, coverage 2. Its only extensive component is ink-vetoed (15a), so it retreats and
**the pool is empty**. Not a pool problem.

## 4. Coverage post-union

**0×1, 1×11, 2×5, 3×7, 4×3**; decided **0/27** (wp14: 0×2,1×10,2×6,3×7,4×2; 1/27). Still inert; decided
fell because `91a16672c4`'s coverage win is now reached by mass alone.

## 5. Verification

**91/91** (+4: feasibility vs published not self; contract floors still hard for components; supersede
direction; item-3's shape synthetic). Anchors updated with traces, not re-baselined silently. `tsc
--strict` clean but the two pre-existing contract errors. **Scorecard 0 violations, 3 sets.** Robustness
not run. Types: `wp15-types.md`.

## 6. Owed ruling

The scale mixing; neither column holds `908479200b`. Unmeasured third option: 15b's ink instrument at
**component** level (these regions measure as grounds, not ink) as the fg preference — 15b was deferred
for want of a mark-level support; a component has one.
