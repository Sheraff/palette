# W-P13 — v0.7.1: ramp-path excursion (decision 17)

## 1. The two motivating covers

| cover | path→chord | published | stop | beyondEnds |
|---|---|---|---|---|
| `16a8247378` (item 1) | **2.573 bars** | **1.213** | `#5c4d4a` @ 0.413 | 0.507 |
| `4130886c02` (item 4) | **0.99977 bars** | — | none | **1.643** |
| `908479200b` (sunset) | 1.165 | 0.625 | `#e06d68` @ 0.760 | 0.551 |

**Item 1 bends and gains a stop**; roles byte-identical. **Item 4's path *is* the chord** — 0.02%
under the trigger. Its 1.64-bar deviation lies entirely *past the surface end*: ends cut short of the
field, which no interior stop can fix (W-P10's premise correction). Decision 18's shape, not 17's.

## 2. Deltas — 5 of 27, four gradient-only

`9021f65e` +`#f1d8c4`@0.79 (1.655→0.651) · `16a8247378` (2.573→1.213) · `908479200b` (1.165→0.625) ·
`ed77a9cb` +`#433d49`@0.72 (1.697→1.003) · `757a78343d` +`#b84b06`@0.40 (6.999→3.183) **plus fg/accent
swap** `#8c3406`↔`#d0d749`, the new ramp moving decision 13's over-ramp APCA floor. **22
byte-identical**, incl. both round-2-fresh. Of 16 gradients, **0→5 carry an interior stop**; the other
11 chords follow their path to within the bar.

## 3. Four deviations, all measured

1. **`g(t)` is arm-f §2.6's empirical path** (field-weighted pixels, equal-mass bands, the fit's own
   weighted mean). The literal "fitted surface along the axis" is provably the chord — pinned by test.
2. **Guard = band-mean standard error (0.04–0.20 bars), not half-widest-gap**: that gap does not
   shrink 64→256 bands (1.22→1.10), so it is a jump in the field, not sampling noise; it refused item
   1's 1.36-bar reduction.
3. **Only the rendered span counts** (`rampContinuity`'s convention); `beyondEnds` reported. Without
   it, two stops passed where the in-span path sat 0.19/0.69 bars off.
4. **`bestGuideStop` (exported) is re-run post-snap by `candidate.ts`, not re-checked** — a sub-bar end
   move shifts this quantity >1 bar (item 1: 1.90→2.57); arm-f-r3 §2.4 literally. It returns an
   occupied triple: no second snap.

`STOP_SPACING_MIN_FRACTION = 0.10` `[UNCALIBRATED]`, 3.2× the 3.1% banding complaint.

## 4. Verification

**68/68 tests** (+6). **Scorecard 0 violations, 3 sets.** `tsc --strict` clean but the pre-existing
contract errors. **Cost 1.22×** (1.48× before deferring the reported occupied excursion to the
winner). Robustness not run.

## 5. Open

Item 4 sits on the trigger to five decimals. Types proposal in `wp13-types.md`.
`round-3/NOTES-cross-arm.md` was changed in this worktree by someone else.
