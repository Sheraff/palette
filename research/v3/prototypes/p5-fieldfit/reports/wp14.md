# W-P14 — v0.8.0: joint four-role assignment with set coverage (decision 18)

## 1. Search space, deltas, cost

`K = 5`, `F = 4`, both `[UNCALIBRATED]`, stated. Field roles are **fixed inputs** (ramp-beats-second-
component decides the surface upstream), so the space is `fg × (accent ∪ collapse)` = **30
assignments**, ≤ 10⁴ by three orders. **26 of 27 byte-identical; 1 delta**: `91a16672c4` fg
`#c39170`→`#d8a685`, `coverageDecided: true`, coverage 2→3. **All three silent STRONGs byte-identical**
(`908479200b`, `a8942d6547`, `eaed77a9cb`). Scorecard **0 violations, 3 sets**. **87/87 tests** (+19).
`readIdentitySet` 10.1 ms mean, 41.6 ms worst (100 211 triples → 121 families).

**One deviation, forced.** A single top-K accent shortlist truncates *before* the pairwise twin
exclusion, so where the heaviest clusters are all one family the shortlist is the fg's own twins and
the accent collapses — **5 of 27** lost legal accents. Shortlists are therefore per-foreground, after
the constraint; `perRoleOnly` then reproduces v0.7.1 exactly.

## 2. Anchors — (a) and (b) are negatives, measured

**(a) item 3 `2376a6b67d`: the red does NOT claim the accent.** Palette unchanged
(`#fad107`/`#f9fbf8`/`#000000`/`#000300`). Coverage *sees* the red — identity family 3, 9.98% — but
**no assignment reaches it**: the red is a field-like *component* (support 0.138), so its overlay mass
is 34.3 against black's 1424 and it is `sameColor` with its own local field — infeasible as an ink
candidate **at any K**. Every assignment ties at coverage 2. Three field-like components, two field
slots.

**(b) item 6 `9646be9b20`: unchanged**, cream/cream/blue/black, coverage 2, not coverage-decided.

## 3. Why coverage is nearly inert

Coverage distribution (27): **0×2, 1×10, 2×6, 3×7, 4×2**; `coverageDecided` **1/27**. Cause:
bar-neighbourhoods fragment one visual colour. Item 3's black holds **7.80%** across **70** families,
largest 1.58% at **rank 5** — outside a four-family set; its set reads yellow/white/red/**yellow'**;
the delta's families 1, 2, 4 are three shades of one tan.

At the twin radius (`ACCENT_FG_EXCLUSION_MULTIPLE = 8` — decision 14's "the bar is far tighter than a
viewer", a fourth strike) item 3's set becomes **exactly the reviewer's four colours** (92→7 families)
and `coverageDecided` rises to **10/27** — but **11 covers move**, including both STRONGs' accents
(`908479200b` `#412824`→`#3b2625`, `a8942d6547` `#009cff`→`#2e00ba`) and `ca2eff2a4a` fg
`#ffffff`→**`#e4e4e4`**, the round-1 UNACCEPTABLE colour. **Not adopted**; default 1,
`P5_IDENTITY_BAR_MULTIPLE` re-runs it. Your call.

## 4. Open

Decision 18 is implemented and correct; it is not sufficient for finding 3. The unanswered piece is
role sourcing: an extensive field-like component carrying no role has no route into any slot. Types in
`wp14-types.md`. Robustness not run.
