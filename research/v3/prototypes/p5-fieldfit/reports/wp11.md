# W-P11 — v0.6.1 cost pass, ratified type moves, mass-floor audit

## 1. Equivalence: byte-identical, all three sets

demo-20 **20/20**, p5-round2-fresh **2/2**, p5-round3-fresh **5/5** — full `palette` JSON vs the
newest committed-state runs (`codeVersion 3dd6266c…`); `diff.ts` 0 changed. No version bump.

## 2. Cost: 2.5–3.3× per palette

**The brief's premise was wrong about where the cost sat.** Component solves already run on the
domain lattice. The profiler found 66% of the pipeline in one function: `median`, sorting up to 9M
residuals, twice per level plus once globally. Two exactly value-preserving fixes: quickselect (the
k-th order statistic *is* the median — same double); and `fitComponentLevel` comparing its two
starts on `supportPixels` alone, so the losing start pays a count pass (`countClaim`), not a full
`measureComponent`. Same comparison, tie rule, winner, measurement.

| cover | size | palette before → after | recursion |
|---|---|---|---|
| `28279e9184` | 300² | 902 → **353** ms | 708 → 244 |
| `fc8d58e0af` | 300² | 872 → **311** | 619 → 157 |
| `2376a6b67d` | 300² | 688 → **353** | 366 → 110 |
| `908479200b` | 300² | 629 → **232** | 453 → 118 |
| `91a16672c4` | 640² | 3200 → **969** | 2862 → 757 |
| `03dbe352…` 3000² (robustness scale) | | 10381 → **4216** | 8344 → 3070 |

Largest remaining term is `modalSeed`'s per-pixel `Map`, ~25% of the recursion; left alone (a
hand-rolled hash risks correctness for ~0.4 s).

## 3. Mass-floor audit — exposure claim **holds**

Share of total overlay mass, floor 1e-4. "Triple" is what the floor actually gates.

| cover | colour | triple ×floor | cluster share | ×floor |
|---|---|---|---|---|
| `2376a6b67d` | white `#fbfaff` | 5.2× | 6.96e-3 | **69.6×** |
| `28279e9184` | pink `#ff00a0` | 5.0× | 2.86e-2 | 285.8× |
| `fc8d58e0af` | green `#616a69` | — | 1.29e-2 | 129.1× |
| `fc8d58e0af` | brown `#201c13` | — | 1.26e-2 | 125.6× |
| `…f569f953` | white title `#ffffff` | 78.8× | 2.86e-2 | 285.8× |
| `908479200b` | gold `#fed078` | 19.3× | 7.22e-2 | 721.9× |
| `908479200b` | gold `#febf6f` | 6.6× | 4.32e-2 | 432.1× |

Nothing reviewer-named was floor-excluded; ranking missed them. **Caveat, measured:** the floor drops
most *triples* of a family (519/554 white, 1183/1440 pink) holding 51%/56% of family mass.
Concentration carries it; two colours sit at 5× at triple level.

## 4. Types, tests, files

Per `wp10-types.md`: `RampContinuity` and `PairMargin`/`MarginReport` moved into `src/types.ts`;
`Diagnostics` gains `continuity` and `margins`; mirrors deleted from `ramp.ts`/`candidate.ts`.
`overlay.ts` exports `FOREGROUND_MIN_RAW_APCA` / `ACCENT_FG_EXCLUSION_MULTIPLE`; `REPORTED_*` gone.

**53/53 tests, `tsc --strict` clean, zero scorecard violations on all three sets.** Touched:
`src/{fieldfit,types,ramp,overlay}.ts`, `candidate.ts`, `diagnose.ts`, `tests/candidate.test.ts`.
