# W-P12 — v0.7.0: field-candidacy veto + accent visibility floor

## 1. Admitting-rule measurement (decision 16's prerequisite)

Nothing measured accent-vs-field *distance*. Items 1/5/8 passed `sameColor` from both ends (5.15 /
4.51 / 1.46 bars), then the contract's accent clause — a pointwise **conjunction**, so a candidate
deep inside `ACCENT_FUNCTIONAL_DISTANCE` (0.034–0.063 ≪ 0.14591) is admitted on `|raw APCA|` alone.
**`EPSILON_ACCENT_RAW = 2.5` admitted all three**, minima over the ramp **3.11 / 3.01 / 4.28**. The
silent accents cleared it at **18.7 / 49.1 / 84.0**.

## 2. Ink-scale anchor

Medial-axis widths (`2·D−1` at DT local maxima) over round-3 type: fine 1–7 px (items 7, 3) =
0.002–0.023 of the short side; display 31–37 px (item 6) = 0.048–0.058. `INK_SCALE_FRACTION = 0.03`
kills everything under 0.06 — both. Supports close at `INK_TEXTURE_CLOSING_FRACTION = 0.007` first;
raw claim masks off JPEG read mortality 1.00 at every radius.

## 3. Two deviations, both forced by measurement

**15b not adopted.** On bar-neighbourhood clusters the instrument is degenerate: mortality **1.0000 on
60 of 60** floor-clearing candidates (items 4/7/8); lowering the radius inverts it (r=1: type
0.52/0.76, *fields* 0.26/0.58/0.70/0.996); adjacency is ≈1 on global-fit covers, ≈0 on component
covers, and on item 4 ranks droplet shadows (0.99) over the white title (0.70). A cluster is a colour
family, not a mark. **Items 4 and 8 keep their foregrounds — that ask is unanswered.**

**15a's adjacency conjunct is inverted.** A component candidate is not *on* a ground; it competes to
be one. Item 6's type: mortality **0.994 / adjacency 0.000**; item 1's sky, which must keep its field:
**0.977 / 0.214**. Grounds tile, ink floats. Both-high is unreachable on the evidence cover, mortality
alone cannot carry it. Conjunction, no blend: `COMPONENT_INK_MORTALITY = 0.85` (bracket 0.67–0.99),
`COMPONENT_INK_GROUND_ADJACENCY_MAX = 0.10` (bracket 0.02–0.23). Tightest live margin: item 1 depth 0,
spared by adjacency alone at 2.14×.

## 4. Deltas — 11 of 27, ten accent-only

`9646be9b20` bg+surface `#00bffa`→`#fae8d0`, fg `#fde9d0`→**`#01bdfd`**. Accents: `16a8247378`
`#3b3e4f`→`#263143`; `91a16672c4` `#1f3038`→`#3b4c56`; `757a78343d` `#ff2c00`→`#d0d749`; `9021f65e`
`#fdf3ea`→`#2c323e`; `fc8d58e0af` `#444d48`→`#e0d0db`; `94002abc` `#6b7c83`→`#2a2924`; `b967964d`
`#ebebeb`→`#d2d5de`; `739e61bf` `#3e2c20`→`#5a2515`; `4aab8aa7` `#e4e4e4`→`#d3d3d3`; `798808bf`
`#252a2e`→`#2d3236`. round2-fresh **0/2**; all else byte-identical.

## 5. The six diagnosed

| cover | palette | fired |
|---|---|---|
| `9646be9b20` | `#fae8d0`/`#fae8d0`/`#01bdfd`/`#000000` | **15a veto** → pool empty → retreat over `fieldFitWithoutInk`; the type colour is now the foreground |
| `16a8247378` | `#484b5a`/`#5d3f27`/`#fee2ba`/`#263143` | **16**: 0.048→0.105 (bg), 0.086→0.124 (surface) |
| `908479200b` | `#7a545f`/`#fd7b61`/`#fed078`/`#412824` | nothing, byte-identical |
| `4130886c02` | `#45820d`/`#67ad19`/`#1f5a00`/`#8fc556` | nothing; accent already 0.218/0.092 |
| `a8942d6547` | `#120032`/`#0f002a`/`#ffffff`/`#009cff` | nothing, byte-identical; white holds |
| `757a78343d` | `#f2190a`/`#868825`/`#8c3406`/`#d0d749` | **16**: 0.034→0.362, 0.253→0.245 |

## 6. Verification

**62/62 tests** (+9: matched-mass strokes-vs-blob, adjacency polarity, border replication, veto fires,
veto spares a field, `fieldFitWithoutInk`, three accent-floor regressions). `tsc --strict` clean but
the two pre-existing contract errors. **Scorecard zero violations, all three sets.** Cost unchanged
off the `noField` path. Robustness not run, as instructed.

## 7. Open

15b unanswered. Item 6 now reads cream/blue/black: role sourcing fixed, "many colors" not — decision
18's problem. Type proposals in `reports/wp12-types.md`.
