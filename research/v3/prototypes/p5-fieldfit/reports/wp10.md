# W-P10 — v0.6.0, the guide-stop doctrine

## t-continuity: both anchors measured, constant set between them

| anchor | required | middle-band mass |
|---|---|---|
| item 8 `…78343d` | continuous | **0.2982** (the discriminator's own) |
| `…2376a6b67d` | bimodal | **0.0446** (global weights, published chord); **0.0000** on its reading's own weights |

`CONTINUOUS_MIDDLE_BAND_MASS = 1/6` — half the mass an evenly travelled ramp puts in its middle
third: **1.79× below** the continuous anchor, **3.74× above** the strictest bimodal one. Both are
pinned as tests.

## Item 8 publishes a ramp — with 2 stops, and not for the ruling's reason

`#f2190a → #868825`, positions 0/1, residual excursion **0.0046 = 0.30× bar**. **No guide stop
exists here**: the 2.36×-bar excursion sits at `u = 0.000` — the background *target* is 0.0362
off-artwork — and a middle vertex cannot shorten an endpoint's own distance. Brute force over all
55 958 occupied triples: every monotone stop scores exactly 0.036189. The ruling's premise ("best
stop lands ~1.3× bar") does not hold; what refused this cover was the fallback firing whenever
`best` failed, which the discriminator removes. Snapping the ends takes the residual under the bar.

## Blast radius: 2 of 27, same shape both times

| set | cover | v0.5.1 → v0.6.0 |
|---|---|---|
| round-3 | `…78343d` | two-block `#f82f01`/`#98961d` + ac `#d0d749` → **gradient `#f2190a`→`#868825`** + ac `#ff2c00` |
| demo-20 | `…59a69094` | flat `#fafafa`/`#e2e2e2` → **gradient `#ffffff`→`#c9c7c8`** (0.280, 2.08× bar, residual 0.17×) |

round-2 fresh: 0 of 2.

## Tests / scorecard

**53/53** (was 47): discriminator unit; guide stop accepted above the bar (two-lobe fixture 2.77× →
1.75× bar, reduction 1.58× — the deleted ≥2× prong would have refused it); flattest-path tie-break;
S-curve (continuous, nothing helps ⇒ straight ramp, not the fallback); both anchors. `tsc --strict`
clean. **Zero violations** on all three sets. Devloop 20/2/5 ok.

## Margins, item 8

bg×su 10.97× · fg×ac 3.09× · fg×bg 8.83× · fg×su 9.32× · ac×su 11.02× · **ac×bg 1.46×**; fg
min|raw| 20.2/15 = 1.35×; twin ratio 10.04/8 = 1.25×. `#ff2c00` on `#f2190a`: two reds, legal by
0.011 OKLab — round-3 note 6's shape, now visible.

Types proposal: `reports/wp10-types.md`.
