# Round-2 composition inputs (pre-design notes, 2026-08-04)

Gate: all four round-1 problem covers responsive to reviewer notes under v0.3 (pass 6 pending).

Composition inputs so far:

1. **Before/after covers** (round-1 items 1, 3, 5, 6) — the core of the round; each had a specific
   note, each grade tests one v0.2/v0.3 ruling.
2. **`fc8d58e0af`** — reviewer named brown/green as the accent they expect; v0.3's min-dist rule
   should find one. Direct named-prediction check.
3. **2–3 fresh non-demo-20 covers** (coverage-set or gold-30) — one light-field gradient
   (fg-adequacy corroboration on a fresh case of item 1's class), one vivid-accent photograph
   (retreat/identity generalization beyond the two graded).
4. **From the campaign addendum (third round, other prototype):**
   - Accent readability graded against both surface AND background — v0.3's accent floor is
     validated policy; include one cover where the accent's contrast is marginal against *surface*
     specifically, if demo-20/fresh picks offer one.
   - Role-swap note (fg↔accent ordering, not extraction) — include `908479200b` (fg is the vivid
     gold `#fed078`; a reviewer might want it as accent) and read any swap note as ordering
     evidence for the fg/accent assignment rules, not as an extraction failure.
   - A hedged false-gradient note cost zero grade — supports the forced-boolean conservatism
     (missed/flat is cheap, false gradient is not); no round-2 item needed, recorded as prior.
