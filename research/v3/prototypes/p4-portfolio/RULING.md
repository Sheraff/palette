# F3 ruling — batch phase2-pair-023 (m4-selector-vs-best)

**2026-08-11, P4 orchestrator. Decode: 8/8 agreement across three independent joins
(batch-log blinding, salt recomputation, palette-content identity) — `data/m4/DECODE.md`.**

## The ruling: branch (b) fires. F3 FIRES. MECHANISM-FALSIFIED.

ROUND.md branch (b), criterion verbatim: *"P3 preferred on majority → F3 FIRES, portfolio adds
risk without value — MECHANISM-FALSIFIED trajectory."* Measured: P3 preferred **5/8** (strict
majority); elected 3; none 0. Net score −2 against the elected side.

**Robustness of the ruling under the pre-registered reading priors (`NOTES-cross-arm.md`):**
- *Immaterial-in-the-judge's-eyes* could at most discount the two strong/strong items (2 and 6)
  — but the reviewer gave her only two EXPLICIT preferences on exactly those items, so the
  prior's condition (reads as same palette) is not met; and even discounting both, the tally is
  2/6 vs 4/6 — branch (b) fires under every admissible reading.
- *Attribution split* (member extraction vs selector choice): the dominant complaint class — 3
  of 4 substantive comments turn on **gradient warrant** — attaches to the CHOICE, not the
  members: both sides were available on every item, the judge preferred the plainer palette,
  and the currency chose the gradient-publishing one 7/8. This is the currency's own
  preference structure, not a member defect laundered through selection.

## What is falsified, precisely

**Description-length palette pricing as a selection authority.** The currency discriminates
decisively (F1: 0.9% half-clustering; margins to 6.3M bits) and confidently elects what the
judge does not prefer: preference shows no margin dependence (narrow-half net 0, wide-half net
−2, Kendall τ-b −0.342 — the pre-registered read rule declares no margin dependence), so more
bits of confidence do not buy more judge agreement. Explanatory economy measures something real
that is not what the reviewer grades — the same verdict, one level up, that killed P1's
currency at role level. Two description-length mechanisms have now fallen in this campaign to
the same root cause, independently instrumented.

**The prior-art pattern is confirmed a fifth time, in its strongest form:** the selector is
where portfolios die — now shown for a NON-FITTED, member-independent, zero-hand-constant
currency, which was the one property prior failures lacked. F2 never tripped: this selector
failed honestly, with nothing tuned toward the reviewer, which is exactly what makes the
falsification informative rather than a tuning accident.

**Not falsified:** the portfolio structure under some other currency (none is currently
proposed that is non-fitted; proposing one is a new pre-registration, not a patch), and the
members themselves (their records live in their own arms).

## Kept precise

- Preferences were prefilled from grades on 6/8 (server behavior, disclosed); the two explicit
  preferences split 1–1 on the strong/strong ties.
- One reviewer endorsement landed on item 7's P3 side (foreground → `#f1e998`) — warehouse
  ground truth, outside the branches, routed upward.
- Branch (d): zero within-pair family-sharing complaints; the condition existed uncomplained on
  item 7 (both field roles "Pitch Black", collapsed) — consistent with the figure/ground
  family principle.
- Naming watch: no served-name effect established ("Lemon Curd" referenced but not served on
  that item; case-mismatched "beige" inconclusive). Recorded, not claimed.
- The disclosed gradient-presence asymmetry (7/8 vs 4/8) is confounded with the currency's own
  preference — it is PART of the finding (the currency over-publishes structure relative to
  judge warrant), not a blinding artifact: side order was salted and the decode is
  triple-verified.

## Salvage inventory (verified, graftable)

1. **Member integration harness** (`tools/snapshot-member.ts`, `verify-pin.ts`,
   `run-member.ts`): run any arm's candidate from a foreign tree with byte-identity proof —
   bake-off-runner grade.
2. **Role-level disagreement tooling** (`tools/disagreement.ts`) + the M1 finding that real
   mechanisms disagree materially on essentially every cover (all-agree 0/20) — a campaign
   fact any comparison protocol needs.
3. **The currency as a development instrument** (`selector/`): a score-any-palette,
   zero-constant explanation meter with measured decisiveness — useful as a dev-time lens
   precisely because it is NOT the judge's axis (divergence between the two is now a mapped,
   evidenced phenomenon), plus the derived σ-quantization floor with its analytic provenance.
4. **The F2 tripwire pattern** (executable no-untagged-constants test): held through five
   workers; portable to any arm.
5. **Corpus artifacts:** the 220-cover bit table with elections and margins; the M4 decode with
   three-way verification; the two-sided-separation election semantics with regression tests.

## Disposition

Per the campaign standard this prototype is COMPLETE: genuinely attempted against the strongest
available members, judged by the reviewer on a pre-registered question, falsified by its own
instruments' branches, salvage named. P4 stands down pending main-tier disposition.
