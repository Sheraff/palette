# P4 parked — 2026-08-11 (second park)

**Committed state:** SPEC `89b81b4` · M1 `c010d71e` · STATE `a63b118a` · notes `dad3b143`/`37a0f8b3`
· M2 `3d2f970e` · M3 `892ad6a5` · M4 `c924b689` · P2 re-pin `8a5237fe` (all signed G,
branch `proto/p4-portfolio`). Ledger empty; nothing owed.

**Falsifier state at park:** F1 ruled NON-FIRING (0.9% half-clustering, 220-cover corpus).
F2 held through five workers (tripwire armed, 58/58 tests). **F3 is UNRULED** — the round
(batch `phase2-pair-023`) is RELEASED and my analyst was dispatched but had not returned at the
park order; its output may land under `data/m4/` unacted-on. THE MECHANISM'S FATE IS OPEN:
nothing in this tree claims survival or falsification — ROUND.md's branches (a)/(b)/(c) decide
it on the analyst's decoded numbers, not before.

## Resume sequence

1. **Rule F3.** Collect the analyst's output (`data/m4/decode.json` + DECODE.md if landed; else
   respawn with the same brief — it is in the dispatch record). Re-verify the dual-path decode
   agreement myself (batch-log blinding join vs mapping.private.json salt recomputation) before
   accepting any number. Apply ROUND.md's branches by their own written criteria, quoting the
   criterion beside the numbers. Commit the ruling (RULING.md + STATE.md §2/§6 update — the
   judge-validated column changes for the first time, in whichever direction). Report per §7,
   including branch (b) = MECHANISM-FALSIFIED trajectory if that is what the numbers say.
2. **Fold verdict evidence:** served-name effects (naming watch), family-vocabulary reads,
   any outside-branch reviewer comments — into NOTES-cross-arm.md and upward in the §7.
3. **Post-ruling queue, per outcome:** branch (a) → retention-rule pass (P1a's probe fate;
   member set edit proposal) + selector runtime posture note for the bake-off record; branch
   (b) → falsification record per campaign standard with salvage inventory (integration
   harness, disagreement tooling, currency-as-instrument, substrate) and stand-down; branch
   (c) → the pre-registered margin read decides which experiment prices next.

Standing rules: fresh workers collected in-turn; F2 tripwire stays armed; member code
read-only; no constants outside the anchored set + the acknowledged §3.1 derived floor.
