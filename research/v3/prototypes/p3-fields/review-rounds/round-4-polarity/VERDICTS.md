# Round-4 verdicts (phase2-pair-011) — decode and actions

Analyst decode; re-key 1:1 verified (6/6 stems). Darker-bg 2 / lighter-bg 1 / no-pref 3, zero
vetoes, zero confound flags.

## Polarity: branch (d) — convention UNMOVED, no new provenance

Preferences interleave and no candidate variable (deltaL, text presence, gradient) separates
them; branch (f) routes item-124 to minTextContrast and its removal still meets no threshold
(2/0/3). The darker-end-is-background tie-band convention stands as-is: not ratified, not
inverted. Notably, ZERO comments name polarity, background, or surface as a preference reason —
the polarity question the robustness math prices is largely invisible to the reviewer's eye.
Formally no branch authorizes reclassifying polarity flips as cosmetic (needed ≥4 no-pref); they
stay fully instrumented.

## Off-branch yield (the round's real value)

1. **Salience watch FIRED (first P3-specific instance).** item-009: "the Sandalwood color of
   the accent does not seem to be part of the artwork, it might be coming from some dithering
   or texture artifact." A published colour read as compression noise — direct evidence for
   coherence-shaped eligibility (W13 experiment 2) and for EVIDENCE item 6's principle.
2. **Identity-coverage: +1 new count (item-132), 2 repeat-confirmations (130, 168 — same
   covers as round-3, same complaints in substance; counted once, consistency confirmed).**
   item-132 also adds: foreground should match the TYPOGRAPHY yellow ("there is a better pick
   that is more yellow and matches the typography"), and the published blue "would be more of
   a background/surface" — role-assignment evidence.
3. item-124 (gap .00534, the degenerate near-neutral): both sides unreadable (roles share
   colours) + "gradient is causing too much banding" both sides — routes to minTextContrast
   and the ramp work, not to polarity.

## Anomalies (upward)

`purpose: "mechanism"` again (2nd round; ROUND.md pre-registered calibration purpose);
`fundedBy` is prose, not a verdict id — explicitly against staging note 4; matters for
warehouse fundedBy chains. item-065 superseded flip (strong/acceptable pref=a → equal
no-pref, 5 s apart) and item-124's preference is prefilled (derived from unequal grades) —
stats-hygiene notes only.
