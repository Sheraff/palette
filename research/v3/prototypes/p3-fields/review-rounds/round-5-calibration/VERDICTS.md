# Round-5 verdicts (phase2-cal-017) — decode and actions

9/9 graded, 0 vetoes, 0 confounds; purpose shipped as declared (calibration); stem ids served
unrewritten; no leakage. Grades: 1 strong / 3 acceptable / 3 weak / 2 unacceptable (the
campaign's first unacceptables, both attributable below).

## Branch outcomes

- **(a) FIRES — named marks 3/4/3.** The accent redesign + eligibility adoption are
  judge-validated. Cinnamon strong, no note. Purple acceptable with a shade nuance ("the
  accent is darker than the real purple" — the cascade landed in the lump's dark end; a
  within-lump refinement note, not a mechanism complaint).
- **(b) FIRES in the discharging direction — 0/4 artifact-family notes** on the admitted-shape
  covers, including both thin-fill edges. The eligibility risk is discharged for this sample
  (4 covers do not close EVIDENCE item 6; passive watch continues). The admitted covers' actual
  complaints name background/surface/foreground — field roles, not the admitted accents.
- **(d) FIRES — both fresh covers ≤2** ("incorrect background" on the busy cover; false
  gradient on the ink cover). Second independent overfitting signal (W17: unseen 19.5→17.0
  while reviewed froze). Fresh-cover regression is now a measured-and-judged fact.
- (c) does not fire; twin count stays 2.

## The 168 arbitration (swap comparator)

"would work better with an accent in the blue tones" at acceptable — the blue IS published but
in the foreground slot; the reviewer wants it as accent, third round running. The swap's
min-ramp-|APCA|-dominance rule mis-assigns the chromatic mark when it out-contrasts the neutral
fg candidate. Fix direction (next cycle): the swap must weigh WHERE the chromatic mark serves
identity (accent) vs where contrast is structurally owed (fg) — candidate rule: don't swap the
chromatic mark INTO fg when the displaced fg candidate still clears the fg floors; measure
against 168/039 and the black-text covers (identity-outranks-legibility precedent bounds it).

## The front has moved: field roles

6/9 notes name background/surface/foreground correctness or gradient existence; 2/9 name accent
(both named marks, both ≥3); row-2's teal prediction REFUTED as worded (note names background
only — not countable as identity-coverage). Third gradient-existence error recorded ("there is
no gradient in this artwork, it's very clearly all flat", on a 3-stop ramp). CONVERGENCE: the
judge's dominant complaint class (field-role selection on busy/textured covers) and the
robustness attribution (edge/depth substrate, e1, spread-sensitive prevalence) now point at the
SAME component. The substrate experiment is no longer only the robustness lever — it is the top
quality complaint class. Its re-proposal (coupling measurement + pooled non-regression gate) is
the single highest-leverage item this prototype has.

## Actions queued (post-park)

1. Substrate re-proposal cycle (decisive; quality + robustness convergence).
2. Swap role-assignment refinement (168/039 evidence; bounded by identity-vs-legibility).
3. Within-lump cascade shade refinement (purple note) — small.
4. Gradient-existence defect class (3 instances) — fold into substrate cycle's field work.
