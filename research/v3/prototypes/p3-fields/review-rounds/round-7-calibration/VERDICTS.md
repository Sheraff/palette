# Round-7 verdicts (phase2-cal-025) — decode and actions

7/7 graded, 0 vetoes/confounds; mapping/leakage/purpose clean. Grades: 2 strong / 2 acceptable
/ 1 weak / 2 unacceptable.

## Branch outcomes

- **(a) PARTIAL — 3 of 4 re-grades ≥3, iteration does not close fully validated.** 168
  weak→STRONG, empty note — the paired-family arc closes validated, and the 1.09× blues drew
  no complaint (the declared alternate did not fire). Row-0 unacc→STRONG (black fg). Cover-19
  unacc→acceptable (the four names held; assignment contested — see off-branch). Row-4 FLAT at
  unacceptable → per the branch, THE SWAP-RETURN MECHANISM ALONE RE-OPENS, narrowly.
- **(b) Twin watch stays passive on the branch item** (row-1 acceptable, no same-colour note) —
  but row-4's complaint ("accent almost indistinguishable from background", sep 5.42× the bar)
  IS the within-palette twin class: **twin count 2→3**, and at 5.42× the bar it is the
  strongest margins-vs-bars datum yet (EVIDENCE item 11).
- **(c) SPLIT, ruled:** bullet 1 fires (white fg prescription restated; the deferral priced at
  weak — and the reviewer supplied the re-composition: fg white, "accent could be Desert...
  which would also give us better contrast"). Bullet 2 ruled NOT independently fired — the
  Desert suggestion is constructive re-composition attached to the fg prescription, not an
  accent-invisibility complaint (orchestrator ruling on the analyst's two recorded readings).
- **(d) Fresh ≤2 — third fresh failure in four releases; the watch HARDENS.** New complaint
  class, off-branch: SPATIAL-EXTENT ("Firebrick... Eggshell... only present in the very small
  label logo at the bottom") — colours real but negligible on canvas. The flip side of
  coherence eligibility: the judge accepts tiny TITLE-TEXT marks but rejects tiny LOGO marks —
  a semantic distinction; recorded as an honest-limit candidate for pixel machinery.

## Off-branch, retained

Row-1: a ≥3 grade carrying a full re-composition prescription (Parchment accent + a second
purple-pink surface making a field gradient). Row-6: the four-name SET validated at 3, but the
reviewer inverts accent/surface among her own names — assignment contested, ordering
unvalidated (staging note 4 stands). Names-based grading recurs (5 named colours across 3
rows) — the colour-naming surface fact again.

## The re-opened item (narrow, evidence-triple-backed)

Row-4's defect: the floor clause returns the swap but auto-lands the displaced weak neutral in
the ACCENT slot — the same demotion pattern as r2-item-8 (round-2) and now a measured twin
(5.42× bar, indistinguishable to the eye). Contained fix design: on swap-return, the displaced
colour must re-qualify as accent (bar + margins vs BOTH field roles); failing that, accent
collapses to fg (existing sanctioned machinery). Three-evidence backing: r2-item-8, row-4,
EVIDENCE 11. DISPATCH HELD pending main's call — the judged record was framed as complete;
running one more contained cycle vs holding for selection is a scope decision, recommendation:
run it (small, closes the last measured within-palette defect class).
