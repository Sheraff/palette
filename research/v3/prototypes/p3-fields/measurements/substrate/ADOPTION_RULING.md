# Substrate adoption ruling (orchestrator, 2026-08-05)

W13 correctly escalated: the pre-registered rule's two readings disagree, and choosing between
them after seeing the numbers is not a worker's call. Ruling, with reasons:

## 1. `field` mode (coherence field replacing edge/EDT): NOT ADOPTED

The rule's per-arm reading returns ADOPT only because 150-trial per-arm Wilson intervals are
~20 points wide — too wide to detect the regression that the pooled number shows plainly
(perturbation 18.7% → 12.0%, control OUTSIDE the substrate's interval; dither 24.3 → 13.5,
q75 10.8 → 5.4). Picking the permissive reading post-hoc to enable adoption would be the exact
motivated move pre-registration exists to prevent; the conservative reading is the one
consistent with the rule's intent ("no regression beyond noise"). Decisive corroboration:
over all 350 trials the substrate gains 44 → 46 agreements — it MOVES stability between axes
rather than creating any — and its central claim (reduced resolution coupling of the edge
field) was never measured. The design survives as a vehicle (flag retained); re-proposal
requires its own coupling measurement plus a pooled non-regression gate, pre-registered.

## 2. `eligibility` mode (coherence-shaped eligibility replacing the raw population floor):
## ADOPTED as the 0.4.1 default

Separable and measured free: pairs 8.5% vs control 8.0%, perturbation pooled identical
(18.7%), every coverage contract number identical, cost 0.97×. Gains: 18 accent collapses
removed; r2-item-4's purple and 130's cinnamon published at rank 0 — the reviewer-named marks
the old floor blocked; answers the goal-3 candidacy-wall finding, the salience principle, and
the corpus fact (median endorsed exact-triple share 8.89e-5). This adoption is a NEW decision
outside the pre-registered rule (which governed only the combined flip), made on these numbers.
RISK, named: eligibility admits small coherent lumps, the shape item-009's artifact accent
wore (that accent shipped under the OLD floor, so the floor was no protection either) — the 18
newly-published accents are unreviewed. Disposition: round-5 carries a sample of them; the
judge decides. That is what rounds are for.

## 3. Coherence-weighted prevalence (cover-114): REFUTED, not adopted

Measurement inverted the hypothesis: coherence mass is itself a flatness measure and WIDENS
white's lead (1.44 → 1.70). The defect in cover-114 is the bar-count itself; ends-selection
stays an open question with one concrete cover. Recorded; no code default changes.

## 4. Re-attributions accepted

208 and 188: their marks are NOT floor-blocked (rank-0 candidates pass verification and are
still not the named mark; 188's refused downstream). W12's "the floor, not the ordering" claim
is measured wrong for those two — the accent evidence set annotations must reflect that before
round-5 uses them.
