# The instrumented mirror of the runtime is generated by probe/instrument.ts. Rebuild it, never
# commit it: it is a full copy of research/v2-3/ and would read as a second runtime.
.variant/
# Per-worker image lists, regenerated by probe/sweep.ts on every run.
data/chunks/
