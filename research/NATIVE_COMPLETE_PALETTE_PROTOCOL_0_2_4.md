# Native Complete-Palette Protocol 0.2.4

Status: Phase 2 research concurrency freeze for `native-complete-palette-0.2.4-development`. One
sealed Phase 5 development matrix is authorized. Human review, review artifacts, reserve access,
calibration, promotion, canonical changes, and production integration remain unauthorized.

## Predecessor

This protocol succeeds the externally interrupted `native-complete-palette-0.2.3-development`:

- policy SHA-256: `b4515514a754748fe36641a0848a8b120e7b17669d5166e8077d67b69a689030`;
- protocol SHA-256: `f7df35e4e7668d1a9fce44eeaa577a4e02a6b991d36ac2221377495513cf5108`;
- failure SHA-256: `1970bc85d2f5b24af03fbc8bb343a51cdd7ea056c547931095295c1d66d0de6f`;
- rejection SHA-256: `1439f5245b57962132349978bd49a8554bfad9b0befcb3c3b447032ddcdcc820`;
- protocol source SHA-256:
  `3e8a312a3790c534598580fdddbb4f47a9848fbd97e6216ee4ed58a6482767ab`.

Version 0.2.3 completed all 391 base groups without any 600000-millisecond candidate timeout and
completed 62 of 92 diagnostics. The external API shell then terminated the parent at its separate
eight-hour command ceiling before atomic publication. Partial candidate output was not inspected and
the stopped namespace is non-resumable.

## Sole Change

Version 0.2.4 changes only isolated-child scheduling resources:

- child concurrency increases from two to four;
- aggregate declared child RSS ceiling increases from 2684354560 bytes (2.5 GiB) to
  5368709120 bytes (5 GiB);
- each child retains the 600000-millisecond wall limit and 1342177280-byte RSS limit;
- the machine binding remains 14 cores and 96 GiB physical memory;
- the enclosing API command ceiling is 57600000 milliseconds (16 hours).

Each source still executes in a separate process and repeats independently. Scheduling order,
scientific output comparison, certificate bytes, deterministic sorting, and publication are
unchanged. Completion order is not a scientific input.

## Unchanged Science And Audit

All palette and audit behavior remains unchanged from 0.2.3:

- APCA remains finite signed comparative evidence with no magnitude floor;
- complete tuple domains, formulas, thresholds, blocks, Pareto rules, and tie order are unchanged;
- all full main and ablation ledgers remain exact;
- synthetic ablation science remains SHA-256
  `7e82ecb8a1be82bfbaccea9ecf3e59a209826bb5ba72a656ce7182c446201691`;
- canonical preservation, challenger hard semantics, provenance, role, distance, collapse,
  cardinality, diagnostics, changed frontier, and Gate A rules are unchanged.

Policy `native-complete-palette-policy-v2.3`, SHA-256
`f745f80c4bdb6a250850a7955bd64c8c86c8f189da8e8026e5d036539b8d9e28`, differs from v2.2 only by
the declared concurrency and aggregate child ceiling.

## Research Boundary And Custody

This remains an exhaustive research oracle, not a production runtime. Later standalone work must
replace it with a bounded selector or dominance-safe search verified against frozen exact results.

- Candidate: `native-complete-palette-0.2.4-development`.
- Protocol: `native-complete-palette-protocol-v2.4`.
- Canonical: unchanged `region-graph-0.19.0`.
- Roster: unchanged 392 paths and 391 exact-source groups.
- Only roots `images` and `00` are authorized.
- Roots `10` through `14` remain sealed and forbidden.

The closure binds this protocol, its preparer, recursive runtime imports, focused tests, package
files, the threshold-free policy authority, and the complete 0.2.3 stop record. Any further change
requires another candidate identity. Human Pause 1 is reached only after a complete Gate A pass.
