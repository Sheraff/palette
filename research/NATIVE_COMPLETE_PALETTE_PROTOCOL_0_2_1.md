# Native Complete-Palette Protocol 0.2.1

Status: Phase 2 implementation-correction freeze for
`native-complete-palette-0.2.1-development`. One sealed Phase 5 development matrix is authorized.
Human review, review artifacts, reserve access, calibration, promotion, and canonical changes remain
unauthorized.

## Predecessor

This protocol succeeds the technically stopped `native-complete-palette-0.2.0-development`:

- scientific policy SHA-256:
  `cc1a44c481700a009b231a56e4cf0946f282cb68b5a9f8323b0c0ee15ff93552`;
- protocol SHA-256: `76786cae252d99c9a696cdbc641710db85f40993997842d20c055ec93aca407a`;
- failure SHA-256: `828f3aa35d05ea24816d32b8226280dfaa99e2bb6382385360fb38b031b19bab`;
- rejection SHA-256: `d50dcff05759daa140831605b70c4d9a939f083d916f639f898e01e0e1ae1b69`;
- protocol source SHA-256:
  `a2f2aa805507c3dd0604c7dda893abfdad14b479f3fd90690c067628378e8f9b`.

Version 0.2 successfully completed the Maroon 5 group that exposed the invalid APCA floors in 0.1.
It stopped after nine exact-source groups because the evaluator applied challenger-only field
semantics to an unchanged canonical incumbent whose background and surface are both `#671f9f` while
its frozen canonical gradient flag is true.

## Implementation Correction

Version 0.2.1 changes no scientific hypothesis or policy value. Its sole implementation correction
is:

```text
validate collapse, gradient, and minimum field distance as hard constraints
if and only if the selected route is challenger-selected
```

An unchanged route must still:

- return the supplied canonical palette by exact object reference;
- preserve exact canonical bytes and semantic hash;
- report the canonical field state and gradient flag without mutation;
- retain finite, exactly recomputable signed APCA evidence;
- satisfy all certificate, determinism, custody, and resource checks.

Existing canonical structural inconsistencies are comparator facts, not candidate-emitted
violations. They may not be repaired, normalized, or used to admit a challenger. Every selected
challenger remains subject to the complete 0.2 collapse, gradient, field-distance, role-distance,
cardinality, provenance, APCA-finiteness, role-domain, and immutability constraints.

## Frozen Scientific Policy

The complete scientific policy is byte-semantically identical to 0.2:

- APCA has no magnitude admission floor and all four APCA block thresholds are zero;
- signed APCA remains finite comparative evidence in the 27-component Pareto vector;
- the same complete field, foreground, accent, and tuple domains are enumerated;
- generated black/white requires absence of a structurally completable source foreground;
- all 23 non-APCA component thresholds, five noncompensatory blocks, strict incumbent dominance,
  tie order, and eight ablations are unchanged;
- all source provenance, role, distance, collapse, cardinality, and no-repair constraints are
  unchanged.

Policy identity remains `native-complete-palette-policy-v2` with SHA-256
`cc1a44c481700a009b231a56e4cf0946f282cb68b5a9f8323b0c0ee15ff93552`.

## Execution And Custody

- Candidate: `native-complete-palette-0.2.1-development`.
- Protocol: `native-complete-palette-protocol-v2.1`.
- Canonical: unchanged `region-graph-0.19.0`.
- Roster: unchanged 392 paths and 391 exact-source groups.
- Runtime, resource ceilings, schedule, repeat comparison, diagnostics, controls, changed frontier,
  artifact limits, and Gate A rules are unchanged from 0.2.
- Only roots `images` and `00` are authorized.
- Roots `10` through `14` remain sealed and forbidden.

The closure binds this protocol, its preparer, recursive runtime imports, focused tests, package
files, the threshold-free policy authority, and the complete 0.2 stop record. It must verify before
source access. Any further implementation or scientific change requires another identity. Human
Pause 1 is reached only after the complete matrix passes Gate A.
