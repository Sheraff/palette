# Native Complete-Palette Protocol 0.2.3

Status: Phase 2 research resource-envelope freeze for
`native-complete-palette-0.2.3-development`. One sealed Phase 5 development matrix is authorized.
Human review, review artifacts, reserve access, calibration, promotion, canonical changes, and
production integration remain unauthorized.

## Predecessor

This protocol succeeds the resource-stopped `native-complete-palette-0.2.2-development`:

- policy SHA-256: `36c007c65ce6141247a1e236da5580c2d397664022e62b17d14fb1b91d53c588`;
- protocol SHA-256: `061ad322a12aaa079cd3dbbdf2d078576458e37b6c59239bd37190c115ad19e2`;
- failure SHA-256: `108fc8b214566838f519fe69ebed2eb3da3727734d805ab47cf45fb2ba4b1aab`;
- rejection SHA-256: `9771869ec8b450f8034c6afdb5e1051eabb130396ba89c2651939cd8d2539d6e`;
- protocol source SHA-256:
  `fadfc4a8df0fd1561d15b56fa4d77afbf2401667cc02145ce6ef5b1e0669ebf6`.

Version 0.2.2 reproduced the frozen synthetic ablation science exactly and completed the same first
19 exact-source groups as 0.2.1. It then reached the same 120-second stop in the active window
containing `images/skap.jpg`, the largest known exposed graph. A graph-only diagnostic ruled out the
other active `00` source as a comparable domain: it had 39 representatives and 231 field treatments,
versus 54 and 276 for Skap.

## Sole Change

Version 0.2.3 changes only the bounded research wall-time envelope:

- per-extraction child wall-time: `600000` milliseconds instead of `120000`;
- timeout errors bind the exact encoded-source SHA-256 and authorized relative path;
- the parent still kills the timed-out child and all active peers immediately;
- timeout output remains a failure artifact and never authorizes partial candidate inspection.

The per-child RSS ceiling remains 1342177280 bytes (1.25 GiB), concurrency remains exactly two, and
the aggregate declared child ceiling remains 2684354560 bytes (2.5 GiB). Output, transport, artifact,
and stream ceilings are unchanged.

The complete run is expected to take approximately one to three hours. The ten-minute source cap is
a research bound, not a production latency target.

## Unchanged Science And Evidence

All palette and audit behavior remains unchanged from 0.2.2:

- APCA is finite signed comparative evidence with no magnitude floor;
- all complete tuple domains, component formulas, thresholds, blocks, and strict Pareto rules are
  unchanged;
- standard tuples feed independent full main and seven-ablation ledgers;
- `without-connected-family-local` retains its separate full stream;
- all per-ablation attempted, rejected, feasible, witness, admitted, and frontier evidence remains;
- exact legacy synthetic ablation science remains bound to SHA-256
  `7e82ecb8a1be82bfbaccea9ecf3e59a209826bb5ba72a656ce7182c446201691`;
- canonical preservation, challenger-only hard semantics, provenance, role, distance, collapse,
  cardinality, determinism, diagnostics, and Gate A rules are unchanged.

Policy `native-complete-palette-policy-v2.2`, SHA-256
`b4515514a754748fe36641a0848a8b120e7b17669d5166e8077d67b69a689030`, differs from v2.1 only by
the declared 600000-millisecond research wall limit.

## Research And Production Boundary

This remains an exhaustive research oracle, not a production runtime. If it passes Gate A and human
review, later standalone work must replace exhaustive inference with a bounded selector or
dominance-safe search and verify it against frozen exact results. Reserve validation should use that
bounded candidate rather than this oracle.

## Execution And Custody

- Candidate: `native-complete-palette-0.2.3-development`.
- Protocol: `native-complete-palette-protocol-v2.3`.
- Canonical: unchanged `region-graph-0.19.0`.
- Roster: unchanged 392 paths and 391 exact-source groups.
- Only roots `images` and `00` are authorized.
- Roots `10` through `14` remain sealed and forbidden.

The closure binds this protocol, its preparer, recursive runtime imports, focused tests, package
files, the threshold-free policy authority, and the complete 0.2.2 stop record. Any further change
requires another candidate identity. Human Pause 1 is reached only after a complete Gate A pass.
